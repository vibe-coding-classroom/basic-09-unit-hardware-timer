# Hardware Timer Stress Test Report

## 1. 實驗目標
驗證硬體定時器 (Hardware Timer) 是否能在 `loop()` 函數被阻塞的情況下，依然維持精確的執行頻率。

## 2. 測試環境
- 開發板：ESP32 DevKit V1
- 定時器頻率：1Hz (1,000ms)
- 阻塞方式：`delay(5000)`

## 3. 實驗紀錄

| 測試情境 | 預期行為 | 實際行為 | 定時器精準度 (OK/Fail) |
| :--- | :--- | :--- | :--- |
| 無阻塞 loop | LED 每秒閃爍一次 | | |
| 阻塞 loop (5s) | LED 每秒閃爍一次 | | |

## 4. 觀察與結論
### 為什麼硬體定時器不受 `delay()` 影響？
(在此回答你的觀察，並解釋中斷搶佔的原理)

### 數據一致性防護
你在程式中是如何保護共享變數的？使用了哪些關鍵字或技術？
- 
- 
